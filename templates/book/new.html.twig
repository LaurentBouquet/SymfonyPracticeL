{% extends 'base.html.twig' %}

{% block title %}{{ "New Book"|trans }}{% endblock %}

{% block body %}

    <div class="container">

        <h1>{{ "Create new Book"|trans }}</h1>

        {{ include('book/_form.html.twig') }}

        <a class="btn btn-secondary mt-2" href="{{ path('app_book_index') }}">{{ "Back to list"|trans }}</a>

    </div>

{% endblock %}
