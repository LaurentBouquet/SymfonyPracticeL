{% extends 'base.html.twig' %}

{% block title %}>{{"Edit Book"|trans}}{% endblock %}

{% block body %}

    <div class="container">

        <h1>{{"Edit Book"|trans}}</h1>

        {{ include('book/_form.html.twig', {'button_label': 'Update'|trans}) }}

        <a class="btn btn-secondary mt-2" href="{{ path('app_book_index') }}">{{ "Back to list"|trans }}</a>

        {{ include('book/_delete_form.html.twig') }}

    </div>

{% endblock %}
