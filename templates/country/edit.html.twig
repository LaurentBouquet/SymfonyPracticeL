{% extends 'base.html.twig' %}

{% block title %}{{"Edit Country"|trans}}{% endblock %}

{% block body %}

    <div class="container">

        <h1>{{"Edit Country"|trans}}</h1>

        {{ include('country/_form.html.twig', {'button_label': 'Update'|trans}) }}

        <a class="btn btn-secondary mt-2" href="{{ path('app_country_index') }}">{{ "Back to list"|trans }}</a>

        {{ include('country/_delete_form.html.twig') }}

    </div>

{% endblock %}
