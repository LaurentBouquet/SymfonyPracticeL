{% extends 'base.html.twig' %}

{% block title %}{{ "New Country"|trans }}{% endblock %}

{% block body %}

    <div class="container">

    <h1>{{ "Create new Country"|trans }}</h1>

    {{ include('country/_form.html.twig') }}

    <a class="btn btn-secondary mt-2" href="{{ path('app_country_index') }}">{{ "Back to list"|trans }}</a>

    </div>

{% endblock %}
