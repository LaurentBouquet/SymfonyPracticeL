{% extends 'base.html.twig' %}

{% block title %}{{"Edit Author"|trans}}{% endblock %}

{% block body %}

    <div class="container">

        <h1>{{"Edit Author"|trans}}</h1>

        {{ include('author/_form.html.twig', {'button_label': 'Update'|trans}) }}

        <a class="btn btn-secondary mt-2" href="{{ path('app_author_index') }}">{{ "Back to list"|trans }}</a>

        {{ include('author/_delete_form.html.twig') }}

    </div>

{% endblock %}
